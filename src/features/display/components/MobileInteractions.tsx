import { INode } from "../../../types";

interface IMobileInteractionsProps {
    selectedNode: INode | INode[] | undefined;
}

export default function MobileInteractions({ selectedNode }: IMobileInteractionsProps) {
    return <div></div>;
}
